import numpy as np
import matplotlib.pyplot as plt
def y(x):
   y=np.log((x**2+1)*np.exp(-(np.fabs(x))/10),1+np.tan(1/(1+np.sin(x)*np.sin(x))))
   return y
x=np.arange(-10,10.01,0.01)
plt.plot(x,y(x))
plt.grid(True)
plt.show()
